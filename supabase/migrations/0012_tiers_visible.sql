-- =============================================================
-- 0012 质量档位「是否在前台显示」开关
-- 让管理员可逐档控制档位分类的可见性（is_visible）：
--   被隐藏的档位不会出现在客户端的档位筛选、详情切换器与卡片档位标中。
-- 使用 IF NOT EXISTS 兼容已运行过 0011 的库；全新库 0011 已含该列，本步为 no-op。
-- 不开启 RLS（与 0009 关闭 app 表 RLS 的策略一致，前端直连 anon key）。
-- =============================================================

alter table public.tiers
  add column if not exists is_visible boolean not null default true;
